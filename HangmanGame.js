
/* A class that runs the game (like the person at the whiteboard) */
class HangmanGame {
    constructor(wordToGuess) {
        // set up any properties
    }

    /* Returns true if the game is over (the word has been fully guessed), false if it isn't over */
    isOver() {

    }

    /* Takes the guessed letter as a parameter and processes the guess, updating the game with that letter guessed */
    guessLetter(letter) {

    }

    /* returns a string representation of the status of the game for showing to the user */
    render() {
        
    }
}