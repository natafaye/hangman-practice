
/* A class that allows the user to pick a word and pick letters */
class ComputerPlayer {
    constructor() {
        // set up any properties
    }

    /* Pick a word with only alphabetical letters, could be one that was picked previously, in all caps, and return it */
    pickWord() {
        // Probably picking it randomly from WORDS
    }

    /* Pick a letter from the alphabet that hasn't been guessed yet, in all caps, and return it */
    pickLetter() {
        // Probably picking it randomly from LETTERS, make sure it hasn't already been guessed
    }
}